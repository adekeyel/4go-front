// Monetag ad (zone 11234732) muted per user request — it was distracting users
// and is no longer displayed anywhere. Kept as a no-op so existing imports
// (if any) don't break.
export default function MonetagAd() {
  return null;
}
